name: YDI構造チェック

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  check:
    uses: ruiyaegashi/YDI-Shared/.github/workflows/ydi-structure-check.yml@main
    with:
      kind: service
